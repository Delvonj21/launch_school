# Consider the set:

numbers = {1, 2, 3, 4, 5, 5, 4, 3}
print(len(numbers))

# What is the set's length? Try to answer without running the code.
# The length of the set is 5 because if there are duplicate values in a set those values are ignored
