# Use map to create a list of lengths of each string in the original list.

strings = ["for", "scores", "and", "seven", "years", "ago"]
length_of_strings = list(map(len, strings))
print(length_of_strings)
